/**
 * A brand document over @jamiethompson/oklch: the ramps the eye drew, which
 * ramp plays which shadcn role, and the steps moved off the preset. Pure
 * functions from the document to bindings, a verdict per token, and the
 * files that ship. The CLI is `oklch-brand`.
 */

export * from "./brand.js";
export * from "./build.js";
